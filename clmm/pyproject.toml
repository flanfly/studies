[project]
name = "clmm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "maturin>=1.14.1",
    "polars-eth",
    "polars-u256-plugin>=0.2.2",
    "sgqlc>=18",
]

[tool.uv.sources]
polars-eth = { workspace = true, editable = true }

[dependency-groups]
dev = [
    "hypothesis>=6.156.6",
]
